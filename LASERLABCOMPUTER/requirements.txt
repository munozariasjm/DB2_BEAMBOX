# Requirements for the laser-lab PC running LASERLABCOMPUTER/wmServer.py.
# This is a *separate* environment from the DAQ host (which uses the
# top-level requirements.txt).
#
# `pyBristolSCPI` and `Bristol.digital` ship with the Bristol wavemeter SDK
# — install per Bristol's instructions (their pip package is not on PyPI;
# you typically `pip install -e path/to/Bristol/`).

mcculw                # Measurement Computing analog out
# Bristol            # vendor SDK — install from Bristol's distribution, not PyPI
